diff --git a/app/main.py b/app/main.py
index d6c19d9..09ca59c 100644
--- a/app/main.py
+++ b/app/main.py
@@ -17,8 +17,8 @@ from slowapi.errors import RateLimitExceeded
 import redis
 
 
-from app.chains import build_chain, build_partner_chain, build_admin_chain
-from app.mongo_executor import run_mongo_query_from_string
+from app.chains import build_chain, build_partner_chain, build_admin_chain, build_affiliate_chain
+from app.mongo_executor import run_mongo_query_from_string, _sanitize_llm_query_text
 from app.mongo_query_service import execute_safe_query, generate_mongo_query, query_improve
 from app.session_store import ensure_session, get_session_messages,save_message
 from app.auth import AuthContext, get_auth_context
@@ -43,8 +43,9 @@ from app.schema_context import build_context
 from app.mongo_query_service import formatting_output
 from app.prompts import ADMIN_MONGO_PROMPT
 from app.prompts import PARTNER_MONGO_PROMPT, PARTNER_QUERY_NORMALIZER_PROMPT, PARTNER_QUERY_MONGO_GUARD_PROMPT
+from app.prompts import AFFILIATE_MONGO_PROMPT, AFFILIATE_QUERY_NORMALIZER_PROMPT, AFFILIATE_QUERY_MONGO_GUARD_PROMPT
 
-from app.mongo_query_service import generate_mongo_query, generate_pymongo_query, normalize_partner_db_query, validate_partner_query_mongo_alignment
+from app.mongo_query_service import generate_mongo_query, generate_pymongo_query, normalize_partner_db_query, validate_partner_query_mongo_alignment, normalize_affiliate_db_query, validate_affiliate_query_mongo_alignment
 from app.llm import get_llm
 from app.safety_guard import predict_query_safety
 from app.cache_service import CacheUnavailableError, get_cache_service
@@ -118,6 +119,7 @@ app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)
 chain = build_chain()
 partner_chain = build_partner_chain()  
 admin_chain = build_admin_chain()
+affiliate_chain = build_affiliate_chain()
 summary_chain = ChatPromptTemplate.from_messages([
     (
         "system",
@@ -774,7 +776,7 @@ def _process_chat_request(
                         logger.warning("Partner Mongo guard blocked query | session_id=%s reason=%s", session_id, blocked_message)
                         return {"allowed": False, "reply": blocked_message, "session_id": session_id}
                 
-                    result = run_mongo_query_from_string(mongo_code, role="partner")
+                    result = run_mongo_query_from_string(mongo_code, role="partner", actor_id=actor_id)
                     
                     print("=========================================================")
                     print(result)
@@ -823,6 +825,175 @@ def _process_chat_request(
                 payload = {"reply": reply, "session_id": session_id}
                 return payload
 
+
+        elif role == "affiliate":
+
+            index = get_index()
+
+
+            role=auth.role
+            query=data.query
+            session_id=session_id
+            actor_id=auth.user_id
+
+            print(f"Received query from affiliate: role={role}, query={query}, session_id={session_id}, actor_id={actor_id}")
+
+
+            decision = router_chain.invoke({"input": query})
+            route_answer = decision.route
+            route_answer = str(route_answer)
+            print(decision.route)
+
+            
+
+            if route_answer == "db" :
+                    
+                    
+                    query_vector = get_embedding(query)
+
+                    results = index.query(
+                    vector=query_vector,
+                    top_k=10,
+                    namespace= os.getenv("AFFILIATE_PINECONE_NAMESPACE"),
+                    include_metadata=True
+                )
+                    kb1 = index.query(
+                    vector=query_vector,
+                    top_k=1,
+                    namespace= "affiliate_kb6",
+                    include_metadata=True
+                )
+                    SCORE_THRESHOLD = 0.0   # tune between 0.7–0.85
+
+                    print("Knowledge base retrieval result:", kb1)
+
+                    if kb1["matches"] and kb1["matches"][0]["score"] >= SCORE_THRESHOLD:
+                      
+                        kb1 = build_schema_context(kb1)
+                        print("Using business knowledge")
+                    else:
+                        # No relevant knowledge → move to schema retrieval
+                        kb1 = ""
+                        print("No business knowledge, try next source")
+
+
+                    original_query = query
+                    try:
+                        query = normalize_affiliate_db_query(
+                            user_query=query,
+                            affiliate_id=actor_id,
+                            normalizer_prompt=AFFILIATE_QUERY_NORMALIZER_PROMPT
+                        )
+                        print(f"Normalized affiliate DB query: {query}")
+                    except Exception:
+                        logger.exception("Affiliate query normalization failed; using original query")
+                        query = original_query
+
+                    query = f"{query}. context-> affiliate : {actor_id} , role : affiliate"
+
+                    schema_context = build_schema_context(results)
+                    
+                    print("===========================================")
+                    print(kb1)
+                    print(type(kb1))
+                    print("===========================================")
+         
+                    print(f"------- QUERY >>> {query}")
+                    mongo_code = generate_mongo_query(
+                    query,
+                    kb1 + schema_context,
+                    mongo_prompt=AFFILIATE_MONGO_PROMPT
+                )
+                    print("--------->>>>> > > >>Generated MongoDB code:", mongo_code)
+                    if isinstance(mongo_code, str):
+                        mongo_code = _sanitize_llm_query_text(mongo_code)
+                        mongo_code = json.loads(mongo_code)
+
+                    
+
+                    collectionx = extract_collections(mongo_code)
+
+                    print("==========================================")
+                    print(collectionx)
+
+                    print("==================COLLECTION CONTEXT========================")
+
+                    contextt = build_context(collectionx)
+                    print(contextt)
+                    print("==========================================")
+
+                    mongo_code = generate_mongo_query(
+                        query,
+                        contextt
+                    )
+
+                    mongo_code = query_improve(kb1+contextt+query, mongo_code)
+
+                    print(f"FINAL MONGO CODE : {mongo_code}")
+
+                    print("=========================================================")
+
+                    guard = validate_affiliate_query_mongo_alignment(
+                        user_query=query,
+                        mongo_code=mongo_code,
+                        guard_prompt=AFFILIATE_QUERY_MONGO_GUARD_PROMPT,
+                    )
+                    if not guard.get("allowed", True):
+                        blocked_message = guard.get("message") or "Generated query is unsafe or does not match your request."
+                        logger.warning("Affiliate Mongo guard blocked query | session_id=%s reason=%s", session_id, blocked_message)
+                        return {"allowed": False, "reply": blocked_message, "session_id": session_id}
+                
+                    result = run_mongo_query_from_string(mongo_code, role="affiliate", actor_id=actor_id)
+                    
+                    print("=========================================================")
+                    print(result)
+
+                    output = formatting_output(query , contextt, mongo_code, result)
+
+                    if isinstance(output, dict):
+                        affiliate_reply =(
+                            output.get("answer")
+                            or output.get("reply")
+                            or output.get("response")
+                            or json.dumps(output)
+                        )
+                    else :
+                        affiliate_reply = str(output)
+
+                    try :
+                        save_message(
+                            session_id=session_id,
+                            role="user",
+                            content=query,
+                            conversation_id=data.conversation_id,
+                            actual_role=role
+                        )
+
+                        save_message(
+                            session_id=session_id,
+                            role="ai",
+                            content=affiliate_reply,
+                            conversation_id=data.conversation_id,
+                            actual_role=role
+                        )
+                        return {"reply": output['answer'], "session_id": session_id}
+            
+                    except Exception as e:
+                        logger.exception("Failed to save messages for affiliate")
+                        return {"error": str(e)}
+
+            else :
+                response = affiliate_chain.invoke(
+                    {"question": query, "context": ""},
+                        config={"configurable": {"session_id": session_id, "namespace": role}}
+                )
+
+                reply = response.content
+                payload = {"reply": reply, "session_id": session_id}
+                return payload
+
+
+
     
 
     
